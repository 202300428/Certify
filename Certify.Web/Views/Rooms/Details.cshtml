@model Certify.API.Models.Room

@{
    ViewData["Title"] = Model.Name ?? "Room Details";
}

<div class="container mt-4">
    <a asp-action="Index" class="back-link"><i class="bi bi-arrow-left"></i> Back to rooms</a>

    <div class="page-header">
        <div>
            <h2>@Model.Name</h2>
            <p class="subtitle">Room configuration and equipment.</p>
        </div>
        <div class="d-flex gap-2">
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-primary"><i class="bi bi-pencil me-1"></i> Edit</a>
            <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
        </div>
    </div>

    <div class="card detail-card">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Name)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Capacity)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Capacity)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Equipment)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Equipment)</dd>
            </dl>
        </div>
    </div>
</div>
