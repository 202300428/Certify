@{
    ViewData["Title"] = "Access Denied";
}

<div class="container mt-4">
    <div class="card form-card text-center">
        <div class="card-body py-5">
            <div class="mb-3" style="font-size:3rem; color: var(--danger);">
                <i class="bi bi-shield-lock"></i>
            </div>
            <h2 class="mb-2">Access Denied</h2>
            <p class="text-muted mb-4">You don't have permission to view this page.</p>
            <a asp-controller="Home" asp-action="Index" class="btn btn-primary"><i class="bi bi-house me-1"></i> Back to Home</a>
        </div>
    </div>
</div>
